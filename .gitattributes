# Shell scripts must keep LF line endings even on Windows checkouts
# (core.autocrlf=true here would otherwise give run_demo.sh CRLF endings,
# breaking bash execution via git-bash/WSL) -- caught during Task H's
# clean-checkout verification.
*.sh text eol=lf
